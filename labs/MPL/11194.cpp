#include <iostream>
#include <string>
#include <cstring>

using namespace std;

int main() {
    string input;
    cin >> input;
    int strLength = input.length();
    for (int c = 0; c < strLength; c++) {
        if (input[c] != '\0') {
            
        }
    }
}